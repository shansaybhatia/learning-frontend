//Type Inference: The compiler automatically figures out the type based on the context.

let drink = "IcedTea";
let cups = Math.random() > 0.5 ? 10 : "5";

//Type Annotation: Explicitly declaring the exact data type in your code.

let drinkFlavour: string = "Ginger";
